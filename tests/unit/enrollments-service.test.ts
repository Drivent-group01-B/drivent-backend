import enrollmentsService from "@/services/enrollments-service";

//Faltam todos os testes desta service
describe("enrollment service suit tests", () => {
  it("should get enrollment by user id", async () => {
    const result = true;
    
    expect(result).toBe(true);
  });
});
