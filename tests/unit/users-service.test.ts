import userService from "@/services/users-service";

//Faltam todos os testes desta service
describe("user service suit tests", () => {
  it("should get user by token", async () => {
    const result = true;
      
    expect(result).toBe(true);
  });
});
